sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t jungkeung0117/docker-react-app -f dockerfile.dev .

script:
  - docker run -e CI=true jungkeung0117/docker-react-app npm run test --

after_success:
  - echo "Test success"